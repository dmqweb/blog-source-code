var merge = function (arr) {
    // 合并重叠的区间
    arr.sort((a, b) => a[0] - b[0]);
    let pre = arr[0];
    for (let i = 0; i < arr.length; i++){
        
    }
};
console.log(merge([[1,4],[4,5]]));